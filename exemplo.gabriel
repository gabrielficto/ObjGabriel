import gabriel

const int arrSize = 7;
numbers[arrSize] : bi64 = {1.75, 2.0, 2.25, 2.50, 2.75, 3.0};

act main() -> bi32 {
	i : bi32 = 0;

	for(i; i < arrSize - 1; i++)
		Gabriel::println(numbers[i]);
}